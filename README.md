# scraping-food-composition

# Criar e ativar o ambiente virtual

py -m venv .venv



  #### Windows 
* python -m venv venv
* .venv\Scripts\activate

pacotes que instalei pra usar 

$ pip install lxml

pip install beautifulsoup4

Instalando um interpretador (parser)
pip install lxml

interagir com pagina para obter codigo do alimento
pip install -U selenium

pip install pandas




selenium
